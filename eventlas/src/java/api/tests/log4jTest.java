/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package api.tests;

import javax.naming.Context;
import javax.naming.InitialContext;
import javax.servlet.ServletContext;
import org.apache.log4j.Logger;
import org.apache.log4j.PropertyConfigurator;

/**
 *
 * @author phil
 */
public class log4jTest{
    public static void main(String[] args){
        /*PropertyConfigurator.configure(log4jTest.class.getProtectionDomain().getCodeSource().getLocation().getPath()+"log4j.properties");
        Logger logger = Logger.getLogger(log4jTest.class.getName());
        logger.fatal("log4j.properties");*/
    }

}
